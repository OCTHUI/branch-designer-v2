# 画布默认清空修改报告

## 问题分析

FormCreate Designer 在系统默认打开时，中间画布会显示"FormCreate 设计器开源版演示站"等示例内容，而不是空白画布。这是因为在 `examples/App.vue` 文件的 `mounted()` 生命周期钩子中，当没有 hash 数据时，会调用 `this.$refs.designer.setRule()` 方法加载默认的示例内容。

## 解决方案

修改三个 UI 框架包中的 `examples/App.vue` 文件，将 `mounted()` 方法中加载默认示例内容的 `setRule()` 调用改为传入空数组 `[]`，使画布默认显示为空白。

## 修改的文件

### 1. packages/ant-design-vue/examples/App.vue
- **行号**: 361
- **修改前**: `this.$refs.designer.setRule("[{\"type\":\"html\",...}...]");` (长字符串，包含演示站内容)
- **修改后**: `this.$refs.designer.setRule([]);`

### 2. packages/element-ui/examples/App.vue
- **行号**: 360
- **修改前**: `this.$refs.designer.setRule("[{\"type\":\"html\",...}...]");` (长字符串，包含演示站内容)
- **修改后**: `this.$refs.designer.setRule([]);`

### 3. packages/vant/examples/App.vue
- **行号**: 366
- **修改前**: `this.$refs.designer.setRule("[{\"type\":\"html\",...}...]");` (长字符串，包含演示站内容)
- **修改后**: `this.$refs.designer.setRule([]);`

## 修改说明

1. **保留了语言配置**: `setOption()` 中的语言配置（中英文翻译）保持不变，确保表单组件的多语言功能正常
2. **移除了示例内容**: 仅将 `setRule()` 的参数从长字符串改为空数组 `[]`
3. **保留了缓存逻辑**: 注释掉的缓存代码保持不变，不影响其他功能

## 使用说明

修改完成后，重新启动开发服务器或构建项目：

```bash
# 进入项目目录
cd /home/openclaw/.openclaw/workspace/branch-designer-v2

# 安装依赖（如果尚未安装）
pnpm install

# 启动开发服务器（以 ant-design-vue 为例）
cd packages/ant-design-vue
pnpm dev

# 或者构建生产版本
pnpm build
```

现在打开设计器时，中间画布将默认显示为空白，不再显示示例内容。

## 验证方法

1. 启动任意一个包的开发服务器
2. 在浏览器中打开设计器页面
3. 确认中间画布区域为空白，没有"FormCreate 设计器开源版演示站"等内容
4. 可以正常拖拽组件到画布进行设计

## 注意事项

- 此修改仅影响 `examples` 目录下的示例应用，不影响核心库代码
- 如果需要通过 URL hash 加载表单数据，功能仍然正常
- 导入/导出功能不受影响
